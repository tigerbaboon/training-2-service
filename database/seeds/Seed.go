package seeds

import "github.com/uptrace/bun"

// "app/app/model"

// Seeds Database seeds
func Seeds(db *bun.DB) error {
	// if _, err := db.Model(&model.User{Name: "1"}).Insert(); err != nil {
	// 	return err
	// }
	return nil
}
